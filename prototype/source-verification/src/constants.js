'use strict';

const HASH_ALGORITHM = 'sha256';
const OFFSET_UNIT = 'utf8-byte';
const VERIFIER_VERSION = 'tree-a-source-verifier/1.0.0';

module.exports = Object.freeze({
  HASH_ALGORITHM,
  OFFSET_UNIT,
  VERIFIER_VERSION,
});
